name: Label Sync

on:
  schedule:
    - cron: "0 0 * * *"

jobs:
  sync-labels:
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 Run Label Sync
        uses: srealmoreno/label-sync-action@v2
        with:
          token: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
          auto-discover-repos: true
          account-type: org
          owner: inhaus-ai-limited
